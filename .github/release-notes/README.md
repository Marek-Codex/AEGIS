# Release notes

Before pushing a `v*` tag, add `.github/release-notes/<tag>.md` for that
version. The release workflow publishes that note alongside its versioned
downloads and SHA-256 checksums. Keep the wording in AEGIS's plain, practical
voice; the release page is for players, not a commit dump.
